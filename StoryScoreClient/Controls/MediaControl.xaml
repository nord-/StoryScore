<UserControl xmlns:Utils="clr-namespace:StoryScore.Client.Controls.Utils"  x:Class="StoryScore.Client.Controls.MediaControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StoryScore.Client.Controls"
             xmlns:my="clr-namespace:StoryScore.Client"
             mc:Ignorable="d" d:DesignStyle="{StaticResource DesignTimeStyle}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ContextMenu x:Key="SyncContextMenu">
            <MenuItem Header="Sync" Click="SyncContextMenu_Click" />
        </ContextMenu>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Label Content="Folder" VerticalAlignment="Center" />
            <Utils:FolderEntry Text="{Binding Path=FolderPath}" Width="250" Height="22" Description="Folder" />
        </StackPanel>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="ButtonsStackPanel">
                <!--<TextBlock Background="Orange" Text="Heading" TextAlignment="Center" FontWeight="Bold" />
                <WrapPanel >
                    <Canvas Width="100" Height="100">
                        <Button Width="100" Height="100">
                        <Viewbox>
                            <TextBlock Width="100" Text="Sand storm" TextAlignment="Center" TextWrapping="Wrap" />
                        </Viewbox>
                        </Button>
                        <TextBlock Text="&#xf127;" FontFamily="{StaticResource FontAwesomeSolid}" Canvas.Right="5" Canvas.Bottom="5" Foreground="DarkGray"/>
                        <TextBlock Text="&#xf0c1;" FontFamily="{StaticResource FontAwesomeSolid}" Canvas.Right="5" Canvas.Bottom="5"/>
                    </Canvas>
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                    <Button Width="100" Height="100" />
                </WrapPanel>-->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
